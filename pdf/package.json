{
  "name": "pdf",
  "version": "1.0.0",
  "description": "## Hello World TODO - make a generic pdf - setup folder (`npm init -y`) - npm install what we need (e.g. pdfjs-dist) ``` npm install pdfjs-dist --save ``` - read pdf file - see if we can find a string in the file",
  "main": "index.js",
  "dependencies": {
    "pdf-parse": "^1.1.1",
    "pdfjs-dist": "^2.1.266"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
